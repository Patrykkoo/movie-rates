<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body data-is-authenticated="<%- isAuthenticated %>"> 
    <header>
    <div class="header-content">
        <a href="/" class="logo-link">
            <h1>Movierates</h1>
        </a>

        <form action="/" method="GET" class="search-form">
            <input type="text" name="search" placeholder="Szukaj filmu lub reżysera..." class="search-input" value="<%= locals.searchTerm || '' %>">
            <button type="submit" class="search-btn">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M19 11 A 8 8 0 0 1 11 19 A 8 8 0 0 1 3 11 A 8 8 0 0 1 19 11 Z"></path>
                    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                </svg>
            </button>
        </form>

        <nav class="main-nav">
            <% if (isAuthenticated) { %>
                <span class="welcome-user">Witaj, <%= username %>!</span>
                <a href="/profile" class="btn btn-nav">Mój Katalog</a>
                <form action="/logout" method="POST">
                    <button type="submit" class="btn btn-nav btn-logout">Wyloguj</button>
                </form>
            <% } else { %>
                <a href="/login" class="btn btn-nav">Zaloguj się</a>
            <% } %>
        </nav>
    </div>
    </header>
    <main>